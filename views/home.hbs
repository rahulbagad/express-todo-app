<!DOCTYPE html>
<html lang="en">
{{> include}}
  <body>
    {{> navbar}}   
    {{#arr}}
    {{#if task}}
      <div class="row">
        <div class="col s6 m6">
          <div class="card blue-grey darken-1">
            <div class="card-content white-text">
              <span class="card-title">{{task}}</span>
            </div>
            <div class="card-action">
              <a href="/update/{{id}}">Update</a>
              <a href="/delete/{{id}}">Mark as Completed</a>
            </div>
          </div>
        </div>
    {{/if}}
    
    {{/arr}} 
    </div> 
    {{>footer }}
  </body>
</html>
