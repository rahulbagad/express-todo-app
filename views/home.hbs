<!DOCTYPE html>
<html lang="en">
{{> include}}
  <body>
    {{> navbar}}
            
            {{#arr}}
            {{#if task}}
            <div class="row"></div>           
                <p >
                    <div class="col-md-6">
                      <td >{{task}}</td>
                    </div>
                    <div class="col-md-4">
                      <a href="/update/{{id}}" class="btn btn-success" role="button">update</a>
                      <a href="/delete/{{id}}" class="btn btn-danger" role="button">delete</a>
                    </div>
                </p>
            </div>
            {{/if}}
            {{/arr}}
        
     
  </body>
  
</html>
